# Дан список повторяющихся элементов.
# Вернуть список с дублирующимися элементами.
# В результирующем списке не должно быть дубликатов.

import random


len_of_list = 100 # длина списка
min_nums = 0 # минимальное значение
max_nums = 10 # максимальное значение

list_of_nums = []
for i in range(len_of_list):
    list_of_nums.append(random.randint(min_nums,max_nums))

print(list_of_nums)
print(f'Список уникальных значений: {list(set((list_of_nums)))}')
